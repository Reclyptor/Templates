import { DetailedHTMLProps, HTMLAttributes } from "react";

export type ${NAME}Props = {
};

export type StandardProps = Omit<DetailedHTMLProps<HTMLAttributes<HTMLDivElement>, HTMLDivElement>, keyof ${NAME}Props>;
